#include <stdio.h>
#include <math.h>
#include "ui.h"
#include "proc.h"

vent_config update_config(vent_config config) {
  // add code to check for updated config
  // mask config interrupts
  // check results of configuration interrupt routine to see if config has changed
  // if changes, update config
  // unmask config interrupts
  return config;
}

void update_display(vent_config config) {
  return;
}


